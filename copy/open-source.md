# Copy — `/open-source`

**Target reading level:** UK reading age 14–15 / US grade 9–10. This page's audience is segment B
(technical), and writing down to them costs more than the extra grade level does.

**Job (ia §1):** the SECONDARY action — run the free CLI. It doubles as the trial the hosted
product does not have, and it is segment B's only reason to trust anything here.

## Head

`<title>` (44 chars)
> Workler CLI — the open-source version

Meta description (147 chars)
> Workler CLI is MIT-licensed and runs on your machine inside ten AI coding CLIs. The scoring
> logic is readable. A fork of Jobber by santifer.

## H1

> Read it before you trust it

## Answer block (48 words, from query-map §3)

> Workler CLI is the free, MIT-licensed command-line version. It runs inside ten AI coding CLIs on
> your own machine, and the scoring logic — thirty-three modes, eighteen market vocabulary sets —
> is readable in the repository. It is a fork of Jobber by santifer, maintained separately under
> the name Workler.

---

## Section 1 — What it is

*Installs beliefs 4 and 6.*

> The judgment this product sells is a set of instructions a model runs under. You can read all of
> them.
>
> Thirty-three modes. Eighteen market vocabulary sets. Eighty provider modules for reading public
> job feeds. The evaluation logic that produces the A–G report and the 1–5 score is not a service
> call — it is text in a repository, and you can disagree with it in a pull request.

Claim-with-receipt on each number, linking to the directory that contains it.

`[CLIENT INPUT REQUIRED: re-verify these counts against the current checkout before launch. The
figures in brief §1 were taken at the v1.24.0 checkout and the ~1,337 test count at the 2026-08-04
audit baseline. Numbers on a public page get checked.]`

## Section 2 — Run it

*The friction-removing section. Segment B abandons at the first instruction that assumes a signup.*

> There is no account, no key and no signup. Clone it and run it inside the AI coding CLI you
> already use.

Terminal block — real output, not a mock-up. `[CLIENT INPUT REQUIRED: a committed transcript of an
actual run, so this block is a recording rather than an illustration.]`

> It supports ten AI coding CLIs, including Claude Code, Codex, OpenCode, Copilot CLI and Qwen.

CTA: **Open the repository →** — after clicking: the Workler CLI repository on GitHub.

## Section 3 — What it does not do

*Installs belief 1 on the page most likely to be read by someone auditing the claim.*

> It never submits an application, sends an email, or clicks anything. There is a test that fails
> if that stops being true.

Claim-with-receipt → `web/test-submit-guard.mjs`.

## Section 4 — Where it came from

*Required by the fork decision and upstream's trademark policy (brief §1). Placed here, not buried
in a footer link, because segment B will find it anyway and finding it in the footer reads worse.*

> Workler is a fork of **Jobber**, an MIT-licensed project by santifer. The fork is maintained
> separately, under a different name, by a different maintainer. Upstream's name and marks belong
> to upstream; this project uses neither.
>
> Full lineage: **Attribution →** `/attribution`

## Section 5 — Free, and the paid version

*Kills objection 7 before the visitor reaches `/pricing` with it unanswered.*

> The command-line version is free and stays free. The hosted version is paid, and it is paid for
> by the person using it — no employer, no recruiter, no advertiser.
>
> `[CLIENT INPUT REQUIRED: what the hosted tier adds over the CLI — brief §7 Q4. This paragraph
> is the honest answer to "why would I pay for this?" and it cannot be written without that
> boundary. Do not ship a vague sentence here; segment B will read a vague sentence as an admission
> that the answer is "nothing".]`
>
> **See what it costs →**

---

## Alt text

- Terminal block: `Terminal output from a Workler CLI run: it scans a job board, evaluates three
  postings, and prints a report number and score for each.`
